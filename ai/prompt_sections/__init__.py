"""
ai/prompt_sections/
Modular system prompt sections for Artifex360.

Each module exports a build(context) function that returns a prompt section string.
The context dict provides mode, settings, and other state needed for conditional sections.
"""

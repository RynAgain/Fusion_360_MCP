# Deprecated -- Tkinter UI

This directory contains the original Tkinter-based GUI from v0.2.0.
It has been superseded by the Flask web application in the `web/` directory.

The Flask web app is launched via `python main.py` and accessed at `http://localhost:5000`.

These files are retained for reference only and may be removed in a future version.
